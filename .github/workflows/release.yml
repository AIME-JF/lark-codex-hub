name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run check
      - run: New-Item -ItemType Directory -Path release -Force; npm pack --pack-destination release
        shell: pwsh
      - run: gh release create "$env:GITHUB_REF_NAME" (Get-ChildItem release\*.tgz).FullName --generate-notes
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
